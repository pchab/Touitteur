<head>
  <title>Touitteur</title>
</head>

<body>
	{{loginButtons}}
	{{#if currentUser}}
		<div id="unfollow">
			{{> unfollow}}
		</div>
		<div id="follow">
			{{> follow}}
		</div>
		<div id="message-board">
			{{> messageBoard}}
		</div>
	{{/if}}
</body>